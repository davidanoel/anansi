# Sui Network
NEXT_PUBLIC_SUI_NETWORK=testnet
NEXT_PUBLIC_SUI_RPC_URL=/api/sui-rpc
SUI_RPC_URL=https://fullnode.testnet.sui.io/

# Contract IDs (set after deploying contracts)
NEXT_PUBLIC_PACKAGE_ID=0x_YOUR_PACKAGE_ID
NEXT_PUBLIC_ORIGINAL_PACKAGE_ID=0x_YOUR_ORIGINAL_PACKAGE_ID
NEXT_PUBLIC_REGISTRY_ID=0x_YOUR_REGISTRY_ID
NEXT_PUBLIC_YIELD_ENGINE_ID=0x_YOUR_YIELD_ENGINE_ID
NEXT_PUBLIC_COMPLIANCE_ID=0x_YOUR_COMPLIANCE_REGISTRY_ID
NEXT_PUBLIC_PLATFORM_ID=0x_YOUR_PLATFORM_ID
NEXT_PUBLIC_CARIB_TREASURY_ID=0x_YOUR_CARIB_TREASURY_ID

# Token Registry (multi-asset)
# List all deployed commodity tokens, comma-separated
NEXT_PUBLIC_REGISTERED_TOKENS=NUTMEG
# Token config as JSON — mintVault (required) and pool (optional, enables DEX trading)
# ⚠️ MUST be a single JSON env var. Next.js strips dynamic process.env access client-side.
NEXT_PUBLIC_TOKEN_CONFIG={"NUTMEG":{"mintVault":"0x_YOUR_NUTMEG_MINT_VAULT","pool":"0x_YOUR_NUTMEG_POOL"}}

# Google OAuth (for zkLogin)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
NEXT_PUBLIC_REDIRECT_URL=http://localhost:3001/auth/callback

# Shinami (Gas Station + zkLogin Prover) — server-side only
SHINAMI_GAS_STATION_KEY=your_shinami_gas_station_key
SHINAMI_ZKLOGIN_KEY=your_shinami_zklogin_key

# Platform Admin — server-side only, NEVER prefix with NEXT_PUBLIC_
# Export your CLI private key: sui keytool export --key-identity YOUR_ADDRESS --json
ADMIN_PRIVATE_KEY=your_base64_private_key
# Choose any strong password for the platform admin dashboard
PLATFORM_ADMIN_KEY=your_admin_password

# IPFS / Pinata (optional — for warehouse receipt uploads)
PINATA_JWT=your_pinata_jwt
NEXT_PUBLIC_IPFS_GATEWAY=https://gateway.pinata.cloud/ipfs
