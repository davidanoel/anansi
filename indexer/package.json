{
  "name": "spice-indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch index.js",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:wait": "sh -c 'until docker compose exec -T postgres pg_isready -U postgres >/dev/null 2>&1; do sleep 1; done'",
    "db:reset": "docker compose down -v && docker compose up -d && npm run db:wait && node migrate.js",
    "db:logs": "docker compose logs -f",
    "db:migrate": "node migrate.js",
    "start": "node index.js"
  },
  "dependencies": {
    "@mysten/sui": "^1.0.0",
    "cors": "^2.8.0",
    "dotenv": "^17.4.2",
    "express": "^4.18.0",
    "pg-promise": "^11.5.0"
  }
}
